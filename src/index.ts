/**
 * Adds two number together
 * @param a number input
 * @param b number input
 * @returns the computation of the twon inputs number
 */
export const add = (a: number, b: number) => {
  // return number
  return a + b;
};
